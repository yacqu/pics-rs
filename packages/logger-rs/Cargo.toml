[package]
name = "logger-rs"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "logger_rs"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
